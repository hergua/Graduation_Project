spring:
  application:
    name: gateway
#  cloud:
#    config:
#      profile: dev
#      discovery:
#        enabled: true
#        service-id: git-config-server
#      fail-fast: true
#      label: master

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka/