spring:
  application:
    name: gateway
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: git-config-server
      fail-fast: true
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/